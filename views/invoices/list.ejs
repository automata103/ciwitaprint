<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestor de Facturas | Sistema ERP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <style>
        :root {
            --primary-color: #4e73df;
            --success-color: #1cc88a;
            --warning-color: #f6c23e;
            --danger-color: #e74a3b;
            --info-color: #36b9cc;
            --gray-100: #f8f9fc;
            --gray-200: #e3e6f0;
            --gray-300: #dee2e6;
            --gray-600: #858796;
            --dark-color: #2e3a4d;
        }

        body {
            background-color: var(--gray-100);
            color: var(--dark-color);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--gray-200);
        }

        .page-title {
            font-weight: 600;
            color: var(--dark-color);
            font-size: 1.8rem;
        }
        
        .page-description {
            color: var(--gray-600);
            margin-bottom: 0;
            font-size: 0.95rem;
        }

        .card {
            border: 1px solid var(--gray-200);
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.05);
            margin-bottom: 2rem;
            border-radius: 0.75rem;
            overflow: hidden;
        }

        .card-header {
            background-color: #fff;
            border-bottom: 1px solid var(--gray-200);
            padding: 1.25rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .card-title {
            font-weight: 600;
            margin-bottom: 0;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .table thead th {
            border-bottom: 1px solid var(--gray-300);
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.5px;
            color: var(--gray-600);
            padding: 0.75rem 1rem;
            background-color: #f9fafc;
            border-top: none;
        }

        .table tbody td {
            padding: 1rem;
            vertical-align: middle;
            border-top: 1px solid var(--gray-200);
            font-size: 0.9rem;
        }

        .customer-name {
            font-weight: 500;
            color: var(--dark-color);
        }

        .customer-type {
            font-size: 0.7rem;
            font-weight: 500;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
            background-color: #f0f3ff;
            color: var(--primary-color);
        }

        .amount-cell {
            font-family: 'Roboto Mono', monospace;
        }

        .zero-amount {
            color: var(--gray-600);
            font-style: italic;
        }
        
        .badge-status {
            font-size: 0.75rem;
            font-weight: 600;
            padding: 0.4em 0.7em;
            border-radius: 50rem;
            display: inline-flex;
            align-items: center;
        }
        
        .badge-paid {
            background-color: rgba(28, 200, 138, 0.1);
            color: var(--success-color);
        }
        
        .badge-pending {
            background-color: rgba(246, 194, 62, 0.1);
            color: var(--warning-color);
        }
        
        .badge-zero {
            background-color: rgba(108, 117, 125, 0.1);
            color: #6c757d;
        }
        
        .badge-partial {
            background-color: rgba(54, 185, 204, 0.1);
            color: var(--info-color);
        }

        .status-icon {
            margin-right: 0.25rem;
            font-size: 0.8rem;
        }

        .action-btn {
            border: 1px solid var(--gray-200);
            color: var(--gray-600);
            transition: all 0.2s;
            width: 32px;
            height: 32px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 50% !important;
            padding: 0;
        }

        .action-btn:hover {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
            transform: translateY(-1px);
        }

        .total-row {
            background-color: var(--gray-100);
            font-weight: 600;
            border-top: 1px solid var(--gray-300);
        }
        
        .total-row td {
            padding-top: 0.75rem;
            padding-bottom: 0.75rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 0;
        }
        
        .empty-state-icon {
            font-size: 3rem;
            color: var(--gray-200);
            margin-bottom: 1rem;
        }
        
        .card-footer {
            padding: 1rem 1.5rem;
            background-color: white;
            border-top: 1px solid var(--gray-200);
        }
        
        .pagination .page-link {
            color: var(--primary-color);
            border-radius: 0.25rem;
            margin: 0 0.15rem;
        }
        
        .pagination .page-item.active .page-link {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .invoice-id {
            font-weight: 500;
            color: var(--primary-color);
        }

        .filter-controls {
            display: flex;
            gap: 0.75rem;
            align-items: center;
            flex-wrap: wrap;
        }

        .table-hover tbody tr:hover {
            background-color: rgba(78, 115, 223, 0.03);
        }
        
        .payment-details {
            font-size: 0.8rem;
            line-height: 1.3;
        }
        
        .paid-amount {
            color: var(--success-color);
        }
        
        .balance-amount {
            color: var(--danger-color);
        }

        .dataTables_filter, .dataTables_length {
            display: none;
        }

        @media (max-width: 768px) {
            .page-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            
            .card-header {
                flex-direction: column;
                align-items: stretch;
            }
            
            .filter-controls {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid py-4">
        <div class="page-header">
            <div>
                <h2 class="page-title">
                    <i class="bi bi-receipt me-2"></i>Facturas
                </h2>
                <p class="page-description">Gestión integral de facturas del sistema</p>
            </div>
            <div>
                <a href="/invoices/add" class="btn btn-primary">
                    <i class="bi bi-plus-circle me-2"></i>Nueva Factura
                </a>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-list-check"></i>Registro de Facturas
                </h5>
                <div class="filter-controls">
                    <div class="input-group input-group-sm">
                        <span class="input-group-text bg-white border-end-0" id="search-addon"><i class="bi bi-search"></i></span>
                        <input type="text" class="form-control form-control-sm border-start-0" id="search" placeholder="Buscar..." aria-label="Buscar facturas por cliente o ID" aria-describedby="search-addon">
                    </div>
                    <select class="form-select form-select-sm" id="status" aria-label="Filtrar por estado">
                        <option value="">Todos los estados</option>
                        <option value="paid">Pagadas</option>
                        <option value="pending">Pendientes</option>
                        <option value="partial">Parciales</option>
                        <option value="zero">Sin Cargo</option>
                    </select>
                </div>
            </div>
            
            <div class="card-body p-0">
                <% if (invoices.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover table-striped mb-0" id="invoicesTable">
                            <thead>
                                <tr>
                                    <th width="80">ID</th>
                                    <th>Cliente</th>
                                    <th width="110">Fecha</th>
                                    <th width="120" class="text-end">Subtotal</th>
                                    <th width="110" class="text-end">ITBIS</th>
                                    <th width="120" class="text-end">Descuento</th>
                                    <th width="100" class="text-end">ROA</th>
                                    <th width="130" class="text-end">Total</th>
                                    <th width="150">Estado</th>
                                    <th width="140" class="text-end">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% invoices.forEach(invoice => { %>
                                    <tr>
                                        <td><span class="invoice-id">#<%= invoice.id %></span></td>
                                        <td>
                                            <div class="customer-name">
                                                <%= invoice.customer ? invoice.customer.name : 'Sin cliente' %>
                                            </div>
                                            <% if (invoice.customer && invoice.customer.type) { %>
                                                <span class="badge customer-type">
                                                    <%= invoice.customer.type %>
                                                </span>
                                            <% } %>
                                        </td>
                                        <td><%= new Date(invoice.date).toLocaleDateString('es-ES', { day: '2-digit', month: '2-digit', year: 'numeric' }) %></td>
                                        <td class="amount-cell text-end <%= Number(invoice.subtotal || 0) === 0 ? 'zero-amount' : '' %>">
                                            <%= Number(invoice.subtotal || 0) !== 0 
                                                ? Number(invoice.subtotal).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) 
                                                : '-' %>
                                        </td>
                                        <td class="amount-cell text-end <%= Number(invoice.itbis || 0) === 0 ? 'zero-amount' : '' %>">
                                            <%= Number(invoice.itbis || 0) !== 0 
                                                ? Number(invoice.itbis).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) 
                                                : '-' %>
                                        </td>
                                        <td class="amount-cell text-end <%= Number(invoice.discount || 0) === 0 ? 'zero-amount' : '' %>">
                                            <%= Number(invoice.discount || 0) !== 0 
                                                ? Number(invoice.discount).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) 
                                                : '-' %>
                                        </td>
                                        <td class="amount-cell text-end <%= Number(invoice.labor || 0) === 0 ? 'zero-amount' : '' %>">
                                            <%= Number(invoice.labor || 0) !== 0 
                                                ? Number(invoice.labor).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) 
                                                : '-' %>
                                        </td>
                                        <td class="amount-cell text-end fw-bold <%= Number(invoice.total || 0) === 0 ? 'zero-amount' : '' %>">
                                            <%= Number(invoice.total || 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                            <% if (invoice.totalPaid > 0 && invoice.total !== 0) { %>
                                                <div class="payment-details">
                                                    <span class="paid-amount">Pagado: <%= Number(invoice.totalPaid).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %></span>
                                                    <br>
                                                    <span class="balance-amount">Saldo: <%= Number(invoice.balance).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %></span>
                                                </div>
                                            <% } %>
                                        </td>
                                        <td>
                                            <span class="badge badge-status <%= invoice.total === 0 ? 'badge-zero' : 
                                                invoice.isFullyPaid ? 'badge-paid' : 
                                                invoice.totalPaid > 0 ? 'badge-partial' : 'badge-pending' %>">
                                                <% if (invoice.total === 0) { %>
                                                    <i class="bi bi-tag-fill status-icon"></i>Sin Cargo
                                                <% } else if (invoice.isFullyPaid) { %>
                                                    <i class="bi bi-check-circle-fill status-icon"></i>Pagada
                                                <% } else if (invoice.totalPaid > 0) { %>
                                                    <i class="bi bi-pause-circle-fill status-icon"></i>Parcial (<%= ((invoice.totalPaid/invoice.total)*100).toFixed(0) %>%)
                                                <% } else { %>
                                                    <i class="bi bi-clock-fill status-icon"></i>Pendiente
                                                <% } %>
                                            </span>
                                        </td>
                                        <td class="text-end">
                                            <div class="btn-group btn-group-sm" role="group" aria-label="Acciones de factura">
                                                <a href="/invoices/<%= invoice.id %>" class="btn btn-light action-btn" title="Ver detalle" aria-label="Ver detalles de la factura #<%= invoice.id %>">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                                <% if (!invoice.isFullyPaid && invoice.total !== 0) { %>
                                                    <a href="/invoices/<%= invoice.id %>/pay" class="btn btn-light action-btn" title="Registrar pago" aria-label="Registrar pago de la factura #<%= invoice.id %>">
                                                        <i class="bi bi-currency-dollar"></i>
                                                    </a>
                                                <% } %>
                                                <a href="/invoices/<%= invoice.id %>/print" class="btn btn-light action-btn" title="Imprimir factura" aria-label="Imprimir la factura #<%= invoice.id %>">
                                                    <i class="bi bi-printer"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                            <tfoot>
                                <tr class="total-row">
                                    <td colspan="3" class="text-end fw-bold">Totales:</td>
                                    <td class="amount-cell text-end fw-bold">
                                        <%= invoices.reduce((sum, inv) => sum + Number(inv.subtotal || 0), 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                    </td>
                                    <td class="amount-cell text-end fw-bold">
                                        <%= invoices.reduce((sum, inv) => sum + Number(inv.itbis || 0), 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                    </td>
                                    <td class="amount-cell text-end fw-bold">
                                        <%= invoices.reduce((sum, inv) => sum + Number(inv.discount || 0), 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                    </td>
                                    <td class="amount-cell text-end fw-bold">
                                        <%= invoices.reduce((sum, inv) => sum + Number(inv.labor || 0), 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                    </td>
                                    <td class="amount-cell text-end fw-bold">
                                        <%= invoices.reduce((sum, inv) => sum + Number(inv.total || 0), 0).toLocaleString('es-ES', { style: 'currency', currency: 'DOP' }) %>
                                    </td>
                                    <td colspan="2"></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <div class="empty-state-icon">
                            <i class="bi bi-file-earmark-excel"></i>
                        </div>
                        <h5 class="fw-semibold">No hay facturas registradas</h5>
                        <p class="text-muted">Comienza creando una nueva factura</p>
                        <a href="/invoices/add" class="btn btn-primary mt-2">
                            <i class="bi bi-plus-circle me-2"></i>Crear primera factura
                        </a>
                    </div>
                <% } %>
            </div>
            
            <% if (invoices.length > 0) { %>
                <div class="card-footer d-flex justify-content-between align-items-center">
                    <div class="text-muted small">
                        Mostrando <span class="fw-semibold"><%= invoices.length %></span> factura<%= invoices.length !== 1 ? 's' : '' %> en total
                    </div>
                    <nav aria-label="Page navigation">
                        <ul class="pagination pagination-sm mb-0">
                            </ul>
                    </nav>
                </div>
            <% } %>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>
        $(document).ready(function() {
            // Inicializar Lucide Icons
            lucide.createIcons();

            const table = $('#invoicesTable').DataTable({
                responsive: true,
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json'
                },
                // Elimina los elementos por defecto de DataTables para usar los propios
                dom: '<"top"i>rt<"bottom"p>',
                pageLength: 10,
                order: [[2, 'desc']],
                columnDefs: [
                    { orderable: false, targets: [9] },
                    { 
                        type: 'currency', 
                        targets: [3,4,5,6,7],
                        render: function(data, type, row) {
                            if (type === 'display') {
                                return data;
                            }
                            return parseFloat(data.replace(/[^\d.,]/g, '').replace(',', '.'));
                        }
                    }
                ],
                initComplete: function() {
                    const api = this.api();

                    // Mueve los controles de paginación al footer
                    const pagination = $(api.table().container()).find('.dataTables_paginate');
                    $('.card-footer nav').empty().append(pagination);

                    // Conecta el input de búsqueda con el filtro de DataTables
                    $('#search').on('keyup', function() {
                        api.search(this.value).draw();
                    });

                    // Conecta el select de estado con el filtro de DataTables
                    $('#status').on('change', function() {
                        const status = $(this).val();
                        
                        api.column(8).search(status).draw();

                        // Nota: El filtro del estado "Sin Cargo" se podría mejorar para ser más preciso en la lógica. 
                        // La implementación actual busca la cadena de texto "Sin Cargo".
                    });
                }
            });
        });
    </script>
</body>
</html>